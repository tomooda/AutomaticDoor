module AutomaticDoor
imports
    from Sensor all,
    from Motor all
exports all
definitions
operations
    tick : () ==> ()
    tick() ==
        if Sensor`detect()
            then Motor`open()
        elseif Motor`isHalted() and not Motor`isClosing()
            then Motor`close();
end AutomaticDoor